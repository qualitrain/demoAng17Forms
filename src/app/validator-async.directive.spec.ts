import { ValidatorAsyncDirective } from './validator-async.directive';

describe('ValidatorAsyncDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidatorAsyncDirective();
    expect(directive).toBeTruthy();
  });
});
