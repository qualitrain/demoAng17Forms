import { ValidatorTelsEqDirective } from './validator-tels-eq.directive';

describe('ValidatorTelsEqDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidatorTelsEqDirective();
    expect(directive).toBeTruthy();
  });
});
